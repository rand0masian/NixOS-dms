# NixOS-dms

A NixOS configuration using the preconfigured DankMaterialShell dotfiles.

## Installation

1. Clone the repo
2. Navigate to the folder within a terminal
3. Run the command: `sudo nixos-rebuild switch --flake .`
